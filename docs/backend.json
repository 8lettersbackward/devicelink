
{
  "entities": {
    "Buddy": {
      "title": "Emergency Buddy",
      "description": "A human contact for emergency orchestration.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "role": { "type": "string" },
        "priority": { "type": "string", "enum": ["Low", "Medium", "High"] },
        "groups": { "type": "array", "items": { "type": "string" }, "description": "The protocol groups this buddy belongs to." },
        "registeredAt": { "type": "number" }
      },
      "required": ["name", "phoneNumber", "priority"]
    },
    "SecurityGroup": {
      "title": "Security Protocol Group",
      "description": "A reusable group for contact orchestration (e.g., Family, Close Friend).",
      "type": "object",
      "properties": {
        "name": { "type": "string" }
      },
      "required": ["name"]
    },
    "Node": {
      "title": "ESP32 Safety Node",
      "description": "A registered hardware safety device.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nodeName": { "type": "string" },
        "hardwareId": { "type": "string" },
        "status": { "type": "string", "enum": ["online", "offline", "error"] },
        "targetGroups": { "type": "array", "items": { "type": "string" }, "description": "The buddy groups this node alerts." },
        "registeredAt": { "type": "number" }
      },
      "required": ["nodeName", "hardwareId", "status"]
    },
    "SOSStatus": {
      "title": "Master SOS Status",
      "description": "Global SOS trigger data for hardware synchronization.",
      "type": "object",
      "properties": {
        "sosTrigger": { "type": "boolean" },
        "trigger": { "type": "boolean", "description": "Legacy trigger flag for specific hardware logic." },
        "sender": { "type": "string" },
        "nodename": { "type": "string" },
        "timestamp": { "type": "number" },
        "lastWebTrigger": { "type": "number", "description": "Timestamp of the last trigger from the web dashboard." },
        "triggeredByNode": { "type": "string" },
        "latitude": { "type": "number" },
        "longitude": { "type": "number" }
      }
    }
  },
  "auth": {
    "providers": ["email"]
  },
  "firestore": {
    "users/{userId}/buddies/{buddyId}": {
      "schema": "Buddy",
      "description": "Emergency contacts registered to a user."
    },
    "users/{userId}/buddyGroups/{groupId}": {
      "schema": "SecurityGroup",
      "description": "Reusable security protocol groups."
    },
    "users/{userId}/nodes/{nodeId}": {
      "schema": "Node",
      "description": "ESP32 nodes registered to a user."
    },
    "sosSystem": {
      "schema": "SOSStatus",
      "description": "Global SOS trigger status for hardware integration."
    }
  }
}
